https://github.com/numba/llvmlite/issues/542
--- ffi/CMakeLists.txt.orig	2022-07-25 18:58:03 UTC
+++ ffi/CMakeLists.txt
@@ -11,10 +11,12 @@ endif()
   set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fno-rtti -g")
 endif()
 
-find_package(LLVM REQUIRED CONFIG)
+find_package(LLVM 11.0 REQUIRED CONFIG)
 
 message(STATUS "Found LLVM ${LLVM_PACKAGE_VERSION}")
 message(STATUS "Using LLVMConfig.cmake in: ${LLVM_DIR}")
+
+set(CMAKE_POSITION_INDEPENDENT_CODE ON) # otherwise FreeBSD build breaks asking for -fPIC
 
 # Set your project compile flags.
 # E.g. if using the C++ header files
